export class Employee {
    ecode: number;
    ename: string;
    salary: number;
    deptid: number;
    constructor(ecode:number, ename: string, salary:number, deptid: number)
    {
        this.ecode = ecode;
        this.ename = ename;
        this.salary = salary;
        this.deptid = deptid;
    }
}
